<!DOCTYPE html>
<html>
  <head>
    <title><%= t('title') %></title>
    <meta charset="utf-8">
    <%# スマホ対応. %>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
  </head>

  <body>
    <%= render partial: 'layouts/navbar' %>

    <% if notice %>
      <div class="alert alert-success"><%= simple_format(h(notice)) %></div>
    <% end %>
    <% if alert %>
      <div class="alert alert-danger"><%= simple_format(h(alert)) %></div>
    <% end %>
    <%= yield %>

    <footer>
      <div class="container-fluid">
        <div class="row">
          <div class="pull-right">Copyright &copy; 2019 Yuto Iwata</div>
        </div>
      </div>
    </footer>
  </body>
</html>
