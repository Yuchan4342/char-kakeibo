<%# new: 新規作成かどうか表す boolean. %>
<% new ||= false %>
<%= simple_form_for @category, html: { class: 'form-horizontal' } do |f| %>
  <%= f.input :name, label: Category.human_attribute_name('name') %>
  <div class='form-group form-actions' >
    <%= f.button :submit, class: 'btn-primary' %>
  </div>
<% end %>
