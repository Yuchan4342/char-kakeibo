<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2><%= t('.edit_category') %></h2>
      <div>
      <%= render partial: 'form' %>
      </div>
      <%= link_to(categories_path) do %>
      <span class="btn btn-sm btn-default"><%= t('.action.back') %></span>
      <% end %>
    </div>
  </div>
</div>
