<div class="container">
  <div class="row">
    <div class="col-md-12">
      <h2><%= t('.edit_purchase') %></h2>
      <%= render partial: 'form' %>
      <%= link_to(purchases_path) do %>
      <span class="btn btn-sm btn-default"><%= t('.action.back') %></span>
      <% end %>
    </div>
  </div>
</div>
